<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <record id="res_config_settings_view_form" model="ir.ui.view">
        <field name="name">res.config.settings.form.inherit</field>
        <field name="model">res.config.settings</field>
        <field name="inherit_id" ref="purchase_sale_inter_company.res_config_settings_view_form" />
        <field name="arch" type="xml">
            <xpath expr="//div[@id='inter_company_so_from_po']" position="after">
                <field name="propagated_serial_number" class="oe_inline" attrs="{'invisible':['|', ('company_id', '=', False), ('so_from_po', '=', False)]}"/>
                <label
                    string="Propagated Lots in sale from purchase"
                    class="o_light_label"
                    for="propagated_serial_number"
                    attrs="{'invisible':['|', ('company_id', '=', False), ('so_from_po', '=', False)]}"/>
            </xpath>
        </field>
    </record>
</odoo>
