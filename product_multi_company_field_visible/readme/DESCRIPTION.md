Bridge module that exposes the `Own Company` field of
`multi_company_field_visible` on the **product** form for users without
multi-company access. It is auto-installed when both
`multi_company_field_visible` and `product_multi_company` are present.
