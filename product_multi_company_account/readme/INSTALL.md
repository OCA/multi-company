This module automatically installs when both `product_multi_company` and 
`account` modules are present. It modifies the tax security rule to fix 
access issues during product creation in multi-company accounting environments.
