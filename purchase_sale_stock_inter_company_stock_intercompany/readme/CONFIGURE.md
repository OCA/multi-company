Configuration is automatic.

Install both `purchase_sale_stock_inter_company` and
`stock_intercompany`; this module will be auto-installed and the
compatibility layer will be active immediately in both directions.
