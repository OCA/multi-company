Glue module — auto-installed when both `product_pricelist_multi_company` and `sale` are installed.

- Extends the pricelist dropdown on sale orders to include pricelists shared via **Available in Companies**.
- Extends company validation so a sale order can reference a shared pricelist without raising a company-mismatch error.
