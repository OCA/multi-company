<?xml version="1.0" encoding="utf-8" ?>
<data>

    <record id="view_partner_property_form" model="ir.ui.view">
        <field name="model">res.partner</field>
        <field
            name="inherit_id"
            ref="account_invoice_transmit_method.view_partner_property_form"
        />
        <field name="arch" type="xml">
            <field name="customer_invoice_transmit_method_id" position="after">
                <div
                    class="o_form_label"
                    attrs="{'invisible': [('company_id', '!=', False)]}"
                />
                <button
                    name="propagate_multicompany_customer_invoice_transmit_method"
                    type="object"
                    attrs="{'invisible': [('company_id', '!=', False)]}"
                    icon="fa-arrow-right"
                    class="btn-link"
                    confirm="Are you sure? This action will propagate this transmission method to all of your selected companies."
                >Propagate customer invoice transmission method to other companies</button>
            </field>
            <field name="supplier_invoice_transmit_method_id" position="after">
                <div
                    class="o_form_label"
                    attrs="{'invisible': [('company_id', '!=', False)]}"
                />
                <button
                    name="propagate_multicompany_supplier_invoice_transmit_method"
                    type="object"
                    attrs="{'invisible': [('company_id', '!=', False)]}"
                    icon="fa-arrow-right"
                    class="btn-link"
                    confirm="Are you sure? This action will propagate this transmission method to all of your selected companies."
                >Propagate supplier invoice transmission method to other companies</button>
            </field>
        </field>
    </record>
</data>
