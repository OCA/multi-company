This module restricts the visibility of records in a multi-company environment if user have several active companies.

When a user has multiple active companies, by default Odoo allows them to see records from all those companies. This module restricts that behavior, raising an error when the user attempts to access records if several companies are active.

