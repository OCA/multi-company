<?xml version="1.0" encoding="utf-8"?>
<openerp>
  <data>

    <record id="view_order_form_inherit" model="ir.ui.view">
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale_stock.view_order_form_inherit"/>
        <field name="arch" type="xml">
            <field name="order_policy" position="before">
               <field name="holding_company_id" invisible="1"/>
            </field>
            <field name="order_policy" position="attributes">
                <attribute name="attrs">{'readonly': [('holding_company_id', '!=', False), ('order_policy', '=', 'picking')]}</attribute>
            </field>
        </field>
    </record>

    </data>
</openerp>
