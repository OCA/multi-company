Glue module — auto-installed when both `product_pricelist_multi_company` and `website_sale` are installed.

- Extends the website sale security rules to include pricelists shared via **Available in Companies**.
- Restores the original `website_sale` rule domains on uninstall.
